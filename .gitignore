# The binary from `make build`.
/terraform-provider-kubewait
# The filesystem mirror and CLI configuration from `make mirror`.
/.mirror/
/dev.tfrc
# The test binary, image context and kubeconfig from `make testincluster`.
/.incluster/
